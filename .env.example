VITE_SUPABASE_URL=
# Use the browser-safe publishable/anon key only. Never use a service-role key.
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_ANON_KEY=
# Optional: set this to the exact deployed origin when email links should return there.
# VITE_APP_URL=https://your-deployed-domain.example
